public class App {
    public static void main(String[] args) throws Exception {
        character Bob = new character("Bob", 10, 10, "Shortsword", 10, 10, 10, "Enemy");
        // Bob.getInfo();
    
        character caleb = new character("Caleb", 10, 10, "Sword", 10, 10, 10, "Player");
        // caleb.getInfo();

        character Jayden = new character("Jayden", 30, 30, "Machete",5, 7, 5, "Player");
        // Jayden.getInfo();

        character Mimo = new character("Mimo", 20, 20,"SwitchBlade", 5, 13, 15, "Enemy");
        // Mimo.getInfo()

        

    

    }
}
